# SCANN Native Annotation 部署指南

## 📦 部署包已准备完成

位置：`G:\wksp\SCANN_v2\docker\scann-deployment-*.tar.gz`

---

## 🚀 快速开始（三步部署）

### 第一步：云服务器 FRP 配置

SSH 连接到云服务器，执行以下命令：

```bash
# 1. 安装 Docker
sudo apt update && sudo apt install -y docker.io docker-compose-plugin
sudo systemctl start docker && sudo systemctl enable docker

# 2. 下载 FRP Server
cd ~
wget https://github.com/fatedier/frp/releases/download/v0.52.3/frp_0.52.3_linux_amd64.tar.gz
tar -zxvf frp_0.52.3_linux_amd64.tar.gz
cd frp_0.52.3_linux_amd64

# 3. 创建配置（修改 token）
cat > frps.toml << 'EOF'
bindPort = 7000
vhostHTTPPort = 80
vhostHTTPSPort = 443
auth.token = "your-very-strong-random-token-change-this"
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "another-strong-password"
EOF

# 4. 创建 systemd 服务
cat > /etc/systemd/system/frps.service << 'EOF'
[Unit]
Description=FRP Server Service
After=network.target

[Service]
Type=simple
User=root
Restart=on-failure
RestartSec=5s
ExecStart=/root/frp_0.52.3_linux_amd64/frps -c /root/frp_0.52.3_linux_amd64/frps.toml
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
EOF

# 5. 启动服务
sudo systemctl daemon-reload
sudo systemctl start frps
sudo systemctl enable frps

# 6. 开放防火墙端口
sudo ufw allow 7000/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 7500/tcp
```

---

### 第二步：本地 OpenWRT 配置

#### 2.1 安装 Docker

```bash
opkg update
opkg install docker docker-compose dockerd dockerd-compose
/etc/init.d/dockerd start
/etc/init.d/dockerd enable
```

#### 2.2 配置 FRP 客户端（通过 Luci 界面）

1. 访问 OpenWRT Web 界面：http://192.168.1.1
2. 导航到：Network → FRPC
3. 添加配置：

**General Settings:**
- Enabled: ✓
- Server Address: `<云服务器公网IP>`
- Server Port: `7000`
- Auth Token: `your-very-strong-random-token-change-this`

**Proxy - Backend:**
- Name: `scann-backend`
- Type: `HTTP`
- Local IP: `127.0.0.1`
- Local Port: `8000`
- Custom Domains: `scann-backend.yourdomain.com`

**Proxy - Frontend:**
- Name: `scann-frontend`
- Type: `HTTP`
- Local IP: `127.0.0.1`
- Local Port: `80`
- Custom Domains: `scann.yourdomain.com`

---

### 第三步：部署应用

#### 3.1 传输部署包到 OpenWRT

在本地 Windows 上执行：

```powershell
# 使用 SCP
scp G:\wksp\SCANN_v2\docker\scann-deployment-*.tar.gz root@<openwrt-ip>:/root/scann-deploy/

# 或使用 WinSCP 上传到 /root/scann-deploy/
```

#### 3.2 在 OpenWRT 上部署

SSH 连接到 OpenWRT：

```bash
ssh root@<openwrt-ip>

# 创建并进入部署目录
mkdir -p /root/scann-deploy
cd /root/scann-deploy

# 解压文件
tar -xzf scann-deployment-*.tar.gz
rm scann-deployment-*.tar.gz

# 配置环境
cp .env.example .env
vi .env  # 根据需要修改（通常保持默认即可）

# 创建数据目录
mkdir -p dataset/new dataset/old dataset/new_marked dataset/annotations logs

# 上传 FITS 文件到 dataset/new/
# 在本地执行：scp "G:\wksp\SCANN_v2\dataset\new\*.fts" root@<openwrt-ip>:/root/scann-deploy/dataset/new/

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 按 Ctrl+C 退出日志
```

---

## ✅ 验证部署

### 1. 检查容器状态

```bash
docker ps
```

应该显示两个容器：
- scann-backend
- scann-frontend

### 2. 测试本地访问

```bash
# 测试后端
curl http://localhost:8000/api/health

# 测试前端
wget -O- http://localhost/health
```

### 3. 检查 FRP 连接

访问云服务器 FRP 管理面板：
- URL: `http://<云服务器公网IP>:7500`
- 用户名: `admin`
- 密码: `another-strong-password`

确认两个代理都是 online 状态。

### 4. 外网访问

配置 DNS 后访问：
- 前端：http://scann.yourdomain.com
- 后端：http://scann-backend.yourdomain.com/api/health

### 5. 登录应用

使用默认账号登录：
- 用户名: `annotator` / 密码: `scann123`
- 管理员: `admin` / 密码: `admin123`

---

## 📚 详细文档

- **QUICK_START.md** - 快速开始指南（推荐新手阅读）
- **COMMANDS.md** - 完整命令清单
- **DEPLOYMENT.md** - 详细部署文档
- **COPY_FILES.md** - 文件复制指南
- **README.md** - 项目说明

---

## 🔧 常用命令

```bash
cd /root/scann-deploy

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 重新构建和启动
docker-compose down && docker-compose build && docker-compose up -d

# 进入容器
docker exec -it scann-backend sh
docker exec -it scann-frontend sh

# 备份数据
tar -czf ../backups/scann-$(date +%Y%m%d).tar.gz dataset/ logs/
```

---

## 🐛 故障排查

### FRP 连接失败

```bash
# 检查云服务器日志
ssh root@<云服务器-ip>
journalctl -u frps -f

# 检查 OpenWRT FRPC 日志
logread | grep frpc

# 重启 FRPC
/etc/init.d/frpc restart
```

### 容器无法启动

```bash
# 查看日志
docker-compose logs backend
docker-compose logs frontend

# 检查端口占用
netstat -tulnp | grep -E '80|8000'

# 重启 Docker
/etc/init.d/dockerd restart
```

---

## 🎉 完成！

恭喜！SCANN Native Annotation 已成功部署。

下次登录时：

```bash
ssh root@<openwrt-ip>
cd /root/scann-deploy
docker-compose ps
docker-compose logs -f
```

访问地址：
- 前端：http://scann.yourdomain.com
- 后端 API：http://scann-backend.yourdomain.com
- FRP 管理面板：http://<云服务器IP>:7500

---

**祝你使用愉快！** 🚀
